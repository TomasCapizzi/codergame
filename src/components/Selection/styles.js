import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container:{
    display: 'flex',
    width: '100%',
    padding: 20,
    alignItems: 'center',
    text:{
      fontSize: 20
    }
  },
})

export default styles;